<%= form_with(model: post_photo, local: true) do |form| %>
  <% if post_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post_photo.errors.count, "error") %> prohibited this post_photo from being saved:</h2>

      <ul>
        <% post_photo.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
